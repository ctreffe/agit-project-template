# Versioned Input Files

This folder contains external files deliberately approved for Git versioning
and assistant access. Review provenance, licensing, sensitivity and repository
need before adding a file.

Git versioning does not by itself authorize publication or other external
sharing. Staging, commits and pushes remain separately controlled actions.
