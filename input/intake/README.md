# Unclassified Incoming Files

Place new or uncertain files here until the maintainer classifies their access,
Git and sharing rules. The folder is ignored except for this README.

An assistant must not enumerate, preview or read these files by default.
Classification begins from maintainer-provided information or separately
approved metadata. Move a file only after its destination is decided.
